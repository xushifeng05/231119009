#include"SqList.h"

/*
void main() {
	SqList list1 , La , Lb , Lc ;
	InitList_sq(list1);
	int i , k , de ,w , ai ,bi;
	ElemType e;
	cout << "请输入初始化的位数：";
	cin >> i;
	list1.length = i;
	cout << "请输入添加的元素中间用空格隔开：";
	for (int j = 0; j < i; j++)
	{
		cin >> list1.elem[j];
	}
	PrintSqList(list1);
	cout << "请输入插入的位序和元素中间用空格隔开：";
	cin >> k >> e ;
	ListInsert_Sq(list1 , k , e);
	PrintSqList(list1);

	cout << "请输入查询的元素：";
	cin >> w;
	ElemType x = LocateElem_Sq(list1,w);
	cout << "查找的元素的位序为：" <<  x << endl;


	cout << "请输入要删除的位序：";
	cin >> de;
	ListDelete_Sq(list1,de,e);
	PrintSqList(list1);

	InitList_sq(La); InitList_sq(Lb); InitList_sq(Lc);
	cout << "输入La的位数：";
	cin >> ai;
	La.length = ai;
	cout << "请输入添加的元素中间用空格隔开：";
	for (int j = 0; j < ai; j++)
	{
		cin >> La.elem[j];
	}
	PrintSqList(La);

	cout << "输入Lb的位数：";
	cin >> bi;
	Lb.length = bi;
	cout << "请输入添加的元素中间用空格隔开:" ;
	for (int j = 0; j < bi; j++)
	{
		cin >> Lb.elem[j];
	}
	PrintSqList(Lb);
	MergeList_Sq(La , Lb , Lc);
	cout << "归并后的Lc为" << endl;
	PrintSqList(Lc);
	cout << "231119009" << "许世烽" << "软工1901";
}*/