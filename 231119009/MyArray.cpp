#include"MyArray.h"

/*int main() {

	
	TSMatrix a, b;
	int i;
	CreateSMatrix(a);
	cout << "原矩阵是：" << endl;
	PrintMatrix(a);
	TransposeSMatrix(a, b);
	cout << "矩阵a经过普通转置后得到矩阵b：" << endl;
	PrintMatrix(b);
	FastTransposeSMatrix(b, a);
	cout << "矩阵b经过快速转置后得到矩阵a：" << endl;
	PrintMatrix(a);
	
	
	CrossList M ,N;
	M.chead = (OLink*)malloc(sizeof(OLink));
	M.rhead = (OLink*)malloc(sizeof(OLink));
	M.mu = 0; M.nu = 0; M.tu = 0;
	N.chead = (OLink*)malloc(sizeof(OLink));
	N.rhead = (OLink*)malloc(sizeof(OLink));
	N.mu = 0; N.nu = 0; N.tu = 0;
	printf("输入测试矩阵M:\n");
	M=CreateMatrix_OL(M);
	printf("输入测试矩阵N:\n");
	N=CreateMatrix_OL(N);
	M = AddSMatrix(M, N);
	printf("矩阵相加的结果为:\n");
	display(M);
	cout << "231119009许世烽" << endl;
    return 0;

}*/